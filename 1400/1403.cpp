#include <stdio.h>

int main()
{
	int a[100], k, i;
	
	scanf("%d", &k);
	for(i = 0; i < k; i++)
		scanf("%d", &a[i]);

	for(i = 0; i < k; i++)
		printf("%d\n", a[i]);
	for(i = 0; i < k; i++)
	
		printf("%d\n", a[i]);
	return 0;
}
